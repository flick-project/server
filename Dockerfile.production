FROM node:24-alpine
WORKDIR /app
COPY --chown=node:node package*.json ./
RUN chown node:node /app
USER node
ENV NODE_ENV=production
RUN npm ci --omit=dev --ignore-scripts
COPY --chown=node:node . .

CMD ["sh", "-c", "npm run migrate:up && exec node src/server.js"]