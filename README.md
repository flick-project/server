# Flick Server

Backend API for Flick, built with Express and PostgreSQL.

## Getting started

1. Clone the repo
2. `npm install`
3. Set up `.env` (see `.env.example` and `.env.test.example`)
4. `npm run dev`

## Testing

`npm test`