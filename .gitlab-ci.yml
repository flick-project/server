.before_script_ssh_setup: &before_script_ssh_setup
  # Ensure ssh-agent is installed and started.
  - command -v ssh-agent >/dev/null || ( apk add --no-cache openssh-client )

  # Start the ssh-agent in the background.
  - eval $(ssh-agent -s)

  # Decode the base64-encoded SSH key and write to a temporary file.
  # Using 'tr' to remove carriage return characters for ed25519 compatibility.
  - echo "$SSH_PRIVATE_KEY" | base64 -d | tr -d '\r' > ~/.ssh_key

  # Set strict permissions (required by OpenSSH) and add key to agent.
  - chmod 600 ~/.ssh_key
  - ssh-add ~/.ssh_key

  # Remove the temporary key file immediately for security.
  - rm ~/.ssh_key

  # Add the remote server to known_hosts to prevent MITM attacks.
  - mkdir -p ~/.ssh && chmod 700 ~/.ssh
  - ssh-keyscan -H $PRODUCTION_HOST >> ~/.ssh/known_hosts
  - chmod 644 ~/.ssh/known_hosts

include:
- template: Security/SAST.gitlab-ci.yml
- template: Security/Secret-Detection.gitlab-ci.yml

variables:
  SECRET_DETECTION_ENABLED: 'true'

default:
  artifacts:
    expire_in: 1 day

# Define the stages of the pipeline and their order of execution.
stages:
  - test
  - deploy

test:
  stage: test
  image: node:24-alpine
  services:
    - postgres:17-alpine
  variables:
    POSTGRES_USER: flick
    POSTGRES_PASSWORD: test
    POSTGRES_DB: flick_test
    DATABASE_URL: postgresql://flick:test@postgres:5432/flick_test
    PORT: 3000
    JWT_SECRET: test-secret
    JWT_LIFE: 10m
    TMDB_API_KEY: fake-key
  script:
    - echo "Running tests and linting..."
    - apk add --no-cache postgresql-client
    - npm ci
    - PGPASSWORD=test psql -h postgres -U flick -d flick_test -f sql/schema.sql
    - node --experimental-test-module-mocks --test --test-concurrency=1 tests/**/*.test.js
    - npm run lint
  # Cache node modules and update if package-lock hash changes.
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - node_modules/
  rules:
    - if: $CI_COMMIT_BRANCH
    - if: $CI_COMMIT_TAG

deploy:
  environment:
    name: production
  stage: deploy
  image: docker:27-cli
  before_script: *before_script_ssh_setup
  script:
    - unset DOCKER_HOST
    - docker context create production --docker "host=ssh://hasse@$PRODUCTION_HOST"
    - docker context use production
    - echo "DATABASE_URL=${DATABASE_URL}" > .env
    - echo "JWT_SECRET=${JWT_SECRET}" >> .env
    - echo "TMDB_API_KEY=${TMDB_API_KEY}" >> .env
    - docker compose -f docker-compose.production.yml up -d --build
    - docker system prune -f
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
  needs:
    - test